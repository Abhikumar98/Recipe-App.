import React from 'react';

const Loading = (props) =>
{
    return(
        <div>
            <h1 style={{textAlign : "center",margin : "auto"}}>Loading...</h1>
        </div>
    )
}

export default Loading